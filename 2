:root {
  --bg-color: #2b3a42;
  --board-color: #2e7d32;
  --cell-border: #1b5e20;
  --black-color: #111;
  --white-color: #fff;
}

body {
  font-family: 'Helvetica Neue', Arial, sans-serif;
  background-color: var(--bg-color);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  margin: 0;
  padding: 10px;
  box-sizing: border-box;
  color: #fff;
  transition: background-color 0.3s;
}

.screen {
  display: none;
  flex-direction: column;
  align-items: center;
  width: 100%;
  max-width: 360px;
}

.screen.active {
  display: flex;
}

h1 {
  margin: 0 0 15px 0;
  font-size: 1.4rem;
  text-shadow: 0 2px 4px rgba(0,0,0,0.5);
}

.menu-card {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(8px);
  padding: 20px;
  border-radius: 12px;
  width: 100%;
  box-sizing: border-box;
  box-shadow: 0 8px 20px rgba(0,0,0,0.3);
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.btn {
  padding: 10px 16px;
  font-size: 0.95rem;
  font-weight: bold;
  color: #fff;
  background-color: #1976d2;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  transition: background 0.2s, transform 0.1s;
  box-shadow: 0 3px 6px rgba(0,0,0,0.2);
}

.btn:hover { background-color: #1565c0; }
.btn:active { transform: scale(0.98); }
.btn-danger { background-color: #d32f2f; }
.btn-danger:hover { background-color: #c62828; }
.btn-ultra { background-color: #d81b60; }
.btn-ultra:hover { background-color: #ad1457; }
.btn-online { background-color: #2e7d32; }
.btn-online:hover { background-color: #1b5e20; }
.btn-secondary { background-color: #555; }
.btn-secondary:hover { background-color: #444; }

.room-input-group {
  display: flex;
  flex-direction: column;
  gap: 6px;
  text-align: left;
}

.room-input-group input {
  padding: 10px;
  border-radius: 6px;
  border: none;
  font-size: 1.2rem;
  text-align: center;
  letter-spacing: 4px;
}

.online-status-text {
  font-size: 0.85rem;
  color: #ffeb3b;
  min-height: 1.2em;
  text-align: center;
}

.setting-group {
  display: flex;
  flex-direction: column;
  gap: 6px;
  text-align: left;
  width: 100%;
  margin-bottom: 10px;
}

.setting-group label {
  font-size: 0.85rem;
  color: #ddd;
}

input[type="range"], select {
  width: 100%;
  padding: 6px;
  border-radius: 4px;
  border: none;
  box-sizing: border-box;
}

.status-panel {
  display: flex;
  gap: 12px;
  margin-bottom: 10px;
  font-size: 0.85rem;
  font-weight: bold;
  background: rgba(0,0,0,0.3);
  padding: 6px 14px;
  border-radius: 20px;
}

.turn-indicator {
  padding: 2px 10px;
  border-radius: 10px;
  background: #555;
}

.turn-indicator.active {
  background: #4caf50;
  color: #fff;
}

.board-container {
  position: relative;
  width: 80vw;
  max-width: 320px;
  height: 80vw;
  max-height: 320px;
}

.board {
  display: grid;
  grid-template-columns: repeat(8, 1fr);
  grid-template-rows: repeat(8, 1fr);
  width: 100%;
  height: 100%;
  background-color: var(--board-color);
  border: 4px solid #111;
  border-radius: 6px;
  box-shadow: 0 6px 16px rgba(0,0,0,0.4);
  box-sizing: border-box;
}

.cell {
  border: 1px solid var(--cell-border);
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  position: relative;
}

.cell.placeable::after {
  content: '';
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background-color: rgba(255, 255, 255, 0.4);
}

.disc {
  width: 82%;
  height: 82%;
  border-radius: 50%;
  transition: transform 0.25s ease;
  box-shadow: 1px 2px 4px rgba(0,0,0,0.4);
}

.disc.black { background-color: var(--black-color); }
.disc.white { background-color: var(--white-color); }

.message-banner {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background: rgba(0, 0, 0, 0.85);
  color: #fff;
  padding: 12px 20px;
  border-radius: 8px;
  font-size: 1.1rem;
  font-weight: bold;
  text-align: center;
  box-shadow: 0 4px 15px rgba(0,0,0,0.5);
  pointer-events: none;
  opacity: 0;
  transition: opacity 0.3s ease;
  z-index: 10;
  width: 80%;
}

.message-banner.show {
  opacity: 1;
}

.sync-overlay {
  position: absolute;
  inset: 0;
  background: rgba(0, 0, 0, 0.6);
  backdrop-filter: blur(2px);
  color: #fff;
  font-size: 1.2rem;
  font-weight: bold;
  display: none;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  z-index: 20;
}

.sync-overlay.active {
  display: flex;
}

.thinking-overlay {
  position: absolute;
  top: 10px;
  right: 10px;
  background: rgba(216, 27, 96, 0.9);
  color: white;
  font-size: 0.75rem;
  font-weight: bold;
  padding: 4px 10px;
  border-radius: 12px;
  display: none;
  animation: blink 0.8s infinite alternate;
  z-index: 5;
}

@keyframes blink {
  0% { opacity: 0.6; }
  100% { opacity: 1; }
}

.analysis-panel {
  width: 100%;
  max-width: 320px;
  margin-top: 10px;
  background: rgba(0, 0, 0, 0.3);
  padding: 8px 12px;
  border-radius: 6px;
  box-sizing: border-box;
}

.analysis-title {
  font-size: 0.7rem;
  color: #ccc;
  margin-bottom: 4px;
  text-align: center;
}

.bar-container {
  height: 10px;
  width: 100%;
  background-color: var(--white-color);
  border-radius: 5px;
  overflow: hidden;
  display: flex;
  margin-bottom: 4px;
}

.bar-black {
  height: 100%;
  background-color: var(--black-color);
  width: 50%;
  transition: width 0.4s ease;
}

.analysis-text {
  font-size: 0.8rem;
  font-weight: bold;
  text-align: center;
}

.nav-btns {
  display: flex;
  gap: 10px;
  margin-top: 10px;
}
